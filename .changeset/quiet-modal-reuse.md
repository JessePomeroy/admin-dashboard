---
"@jessepomeroy/admin": minor
---

Reuse the shared accessible modal shell for email templates and platform-client details while preserving responsive sizing. Remove the unreachable platform-client creation dialog and direct users to operator onboarding.

AdminModal adds an optional accessible label and CSS sizing overrides; existing defaults remain unchanged.
