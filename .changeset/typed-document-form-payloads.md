---
"@jessepomeroy/admin": patch
---

Use precise package-local invoice and contract form payloads, share each document's create mutation mapping between draft and send actions, and remove duplicate contract form assembly. Preserve optional fields, fractional invoice calculations, cent conversions, and separate email overrides.
