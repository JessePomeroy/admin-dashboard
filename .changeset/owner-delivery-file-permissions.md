---
"@jessepomeroy/admin": minor
---

Support owner-only delivery-gallery file uploads through a host-resolved, signed upload policy. Hosts without the optional capability and verifier retain photo/video uploads. Render arbitrary files as file cards and skip image decoding for non-image originals.
